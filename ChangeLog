0.3:
	* Add strlcat again with an autoconf check.
	* Emit decimal numbers when externalizing uint32 instead
	  of hexadecimal.
0.2:
	* A bugfix from NetBSD that fixes two race conditions that
	  could corrupt the red-black tree to store the objects.
	* Added some more tests to configure.ac.
	* Fixed the COPYING file with proper licenses this time.

0.1:
	* Initial import of NetBSD's libprop.
